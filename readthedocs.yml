# Don't build any extra formats
formats:
    - none

requirements_file: docs/requirements.txt

build:
    image: latest

python:
    version: 3
    setup_py_install: true 
    extra_requirements:
        - examples